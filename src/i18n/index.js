/* Language selection.
 *
 * English is the app's language. German is planned as a second language —
 * when it arrives, add de.js and switch on a stored preference here. Every
 * component already imports `t`, so nothing else has to change.
 */
import en from "./en.js";

export const t = en;
export default t;
