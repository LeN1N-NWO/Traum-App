# Copy to .env and fill in. NEVER commit the real .env — this file
# (.env.example) has no real values, so it's safe to commit.

# fal.ai — image generation (Nano Banana 2) AND video generation
# (minimax/h3, image-to-video). Required for any generation to work.
# Key-Format: "KEY_ID:KEY_SECRET", siehe https://fal.ai/dashboard/keys.
FAL_KEY="YOUR_KEY_ID:YOUR_KEY_SECRET"

# DeepSeek — OPTIONAL. Crafts the Nano Banana prompt from the dream text and
# named-reference metadata (DeepSeek is text-only, never sees the actual
# photos — those go straight to fal.ai). Without this key, generation still
# works via a local prompt template, just less richly worded.
# Get a key at https://platform.deepseek.com (API keys).
# DEEPSEEK_KEY="YOUR_KEY"

# Google Gemini — OPTIONAL, for the voice-first dream interview (Gemini Live).
# fal.ai does NOT do realtime conversational voice; this one comes straight
# from Google. Get a key at https://aistudio.google.com/apikey → "Create API
# key". The free AI Studio tier is enough to develop against; the Live model
# in production needs billing enabled on that Google Cloud project.
# GEMINI_KEY="YOUR_KEY"
# The same key also serves the voice previews in the picker (Gemini TTS,
# /api/voice-sample). Each (voice, language) sample is generated ONCE and
# cached under media/, so the TTS cost is a rounding error.

# Optional: override model slugs/names if they differ from defaults.
# Confirmed 08.08.2026 against fal's own validation responses:
#   nano-banana-2      $0.08 / image at 1K
#   minimax/h3         $0.08 / second at 768P, duration 5–15s (re-measured 09.08.2026)
# FAL_MODEL_IMAGE="fal-ai/nano-banana-2"
# FAL_MODEL_VIDEO="minimax/h3/image-to-video"
# DEEPSEEK_MODEL="deepseek-v4-flash"
# GEMINI_TTS_MODEL="gemini-3.1-flash-tts-preview"

# Optional: ein geteiltes Geheimnis vor die teuren Endpunkte legen.
# OHNE diese Zeile bleibt alles offen wie bisher — nur die Mengenbegrenzung
# greift (20 Bilder/Minute je Absender, siehe src/lib/gatekeeper.js). Sobald
# gesetzt, muss der Client den Kopf `x-api-token` mitschicken; ohne ihn
# antwortet der Server mit 401.
#
# ⚠ Das ist KEINE Benutzerverwaltung. Es gibt weiterhin keine Konten und kein
# serverseitiges Guthaben — das Geheimnis schützt den Port, nicht den Nutzer
# vor sich selbst. Vor einer öffentlichen Adresse braucht es beides.
# API_TOKEN="ein-langes-zufaelliges-wort"

# Optional: change port (default 8100)
# PORT=8100
